{
  "version": 2,
  "builds": [
    {
      "src": "apiTT/wsgi.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "50mb" }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/app/wsgi.py" }],
  "env": {
    "DJANGO_SETTINGS_MODULE": "app.settings"
  }
}
