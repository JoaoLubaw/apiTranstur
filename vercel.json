{
  "version": 2,
  "builds": [
    {
      "src": "apiTT/asgi.py",
      "use": "@now/python",
      "config": { "maxLambdaSize": "50mb" }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/app/asgi.py" }],
  "env": {
    "DJANGO_SETTINGS_MODULE": "app.settings"
  }
}
